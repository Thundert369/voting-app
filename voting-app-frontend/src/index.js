import React from 'react';
import ReactDOM from 'react-dom';
// import './index.css'; // You can remove this if you don't have an index.css
import App from './App'; // App component

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
